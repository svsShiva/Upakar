import { myHelpsActionTypes } from "../../../redux/actions/action-types";

const initialState = {
}

const myHelpsReducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default myHelpsReducer;