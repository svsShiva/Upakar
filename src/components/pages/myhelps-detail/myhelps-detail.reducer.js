import { myHelpsActionTypes } from "../../../redux/actions/action-types";

const initialState = {
}

const myHelpsDetailReducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default myHelpsDetailReducer;